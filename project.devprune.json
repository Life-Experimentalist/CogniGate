{
  "$schema": "https://devprune.vkrishna04.me/schemas/v1/devprune.schema.json",
  "prunable": {
    "directories": [
      {
        "path": "docs/.next",
        "rebuild": "npm --prefix docs ci && npm --prefix docs run build",
        "why": "Next.js build cache — regenerated by the docs build; git-ignored via docs/.gitignore"
      },
      {
        "path": "docs/out",
        "rebuild": "npm --prefix docs ci && npm --prefix docs run build",
        "why": "Next.js static export output deployed to GitHub Pages — regenerated by the docs build; git-ignored via docs/.gitignore"
      }
    ]
  }
}
